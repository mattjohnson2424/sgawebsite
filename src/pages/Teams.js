import { Link } from "react-router-dom"

export const Teams = () => {
    return (
        <div>
            <h1>Teams</h1>
            <Link to="/">Back to Home</Link>
        </div>
    )
}

export default Teams;