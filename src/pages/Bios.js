import { Link } from "react-router-dom"

export const Bios = () => {
    return (
        <div>
            <h1>Exec Bios</h1>
            <Link to="/">Back to home</Link>
        </div>
    )
}

export default Bios;